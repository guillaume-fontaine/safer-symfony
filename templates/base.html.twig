<!DOCTYPE html>
<html>
    <head>
    {% block header %}
        {{component('header')}} <!-- A ete rajoute pour rajouter bootstrap a toutes nos template twig-->
    {% endblock %}
    </head>
    
    <body>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        
        {% block body %}{% endblock %}
    </body>
</html>
